// Modules
import { init as stars } from 'modules/stars'  ; // or './module'

// Start the scripts
document.addEventListener("DOMContentLoaded", function(){
    // Handler when the DOM is fully loaded
    stars();
});